# Backend API URL (must match where uvicorn / FastAPI runs)
# Local: same port as backend — default in code is http://localhost:8001
# Production: https://your-backend-url.com

VITE_API_URL=http://localhost:8001

